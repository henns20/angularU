//goals of Gallerypattern
// 1) 


<div class="list-group-item" ng-repeat="product in store.products">
h3>{{product.name}} <em class="pull-right"> {{ product.price | currency }}

<!-- Image Gallery -->
div.gallery ng-show="product.images.length"
	img ng-src="product.images.[gallery.current]"
	ul class="list-inline thunbs" <!--thumbnail inline -->
		li.thumbnail ng-repeat="image in product.images"
		a href ng-click="gallery.setImage()"	
		img ng-src="{{image}}





</div>




// Gallery Controller

(function(){
	var app = angular.module('gemStore' []);

	app.controller('GalleryController', function(){
  this.current = 0;
  this.setCurrent = function(newGallery){
    this.current = newGallery || 0;
  };

})

})();
